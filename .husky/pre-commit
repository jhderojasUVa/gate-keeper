#!/usr/bin/env node
import { execSync } from 'child_process';

try {
  execSync('npm test', { stdio: 'inherit' });
} catch (error) {
  console.error('pre-commit hook failed: npm test');
  process.exit(1);
}
